{
  "name": "adeptio-auth-worker",
  "version": "2.4.1.0",
  "private": true,
  "type": "module",
  "description": "Adeptio v2.4.1.edge.auth — edge identity Worker (Argon2id + LDAPS + RadSec + Turso, server-authoritative).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test test/"
  },
  "dependencies": {
    "hash-wasm": "^4.11.0"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
